spring:
  application:
    name: apna-dukan-backend

server:
  port: 8080

jwt:
  secret: apnadukan-secret-key-change-in-production-minimum-256-bits-required-for-hmac-sha512
  expiration: 86400000 # 24 hours in milliseconds

logging:
  level:
    root: INFO
    com.apnadukan: DEBUG

